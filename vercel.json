{
  "version": 2,
  "builds": [
    {
      "src": "app.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/routes/$1" },
    { "src": "/uploads/(.*)", "dest": "/uploads/$1" }
  ]
}
